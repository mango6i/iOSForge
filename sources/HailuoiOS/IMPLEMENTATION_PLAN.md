# 海螺 iOS 14+ 原生版实施计划

## 不可突破的边界

- Android 源项目 `HailuoAndroid` 与现有后端只读，不修改文件、接口或数据。
- iOS 只写入 `HailuoiOS`，继续复用当前后端 API。
- 最低系统 iOS 14；使用 Swift 6、SwiftUI 与必要的 UIKit 桥接。
- iOS 26 及以后只使用 Apple 标准导航栏、标签栏、工具栏和弹窗提供的原生 Liquid Glass；iOS 14–25 使用系统材质兼容外观。
- Windows 阶段只完成代码、资源、契约检查和文档；Swift 编译、签名、真机测试及 IPA 导出必须在 Mac/Xcode 完成。

## 分阶段计划与当前状态

| 阶段 | 具体工作 | 验收方式 | 状态 |
|---|---|---|---|
| 1. 安卓基线盘点 | 清点页面、路由、接口、数据模型、权限、轮询和危险操作 | Android/iOS 对照表 | 已完成，持续回归 |
| 2. 工程骨架 | XcodeGen、iOS 14 Target、Swift 6、资源、Info、Entitlements、测试 Target | `xcodegen dump/generate` | 代码已完成，待 Mac 验证 |
| 3. 基础设施 | API envelope、鉴权、Keychain、缓存、日期、上传、证书固定、401/踢下线 | 单元测试与网络 Mock | 已完成主体 |
| 4. 登录注册 | 密码/SMS/注册/找回、记住账号、历史账号、注销冷静期、封禁与锁定提示 | 对照 Android 状态流 | 已完成可用后端范围 |
| 5. 消息聊天 | 会话、未读、广播、分页、恢复旧历史、文本/图片/语音/位置/视频壳、引用/撤回/删除 | 逐类型真机测试 | 已完成主体，媒体真机验收待 Mac |
| 6. 好友 | 列表、搜索、详情、认可、备注、赠送、投诉、拉黑、删除、回收站 | 操作后状态立即同步 | 已完成主体，真机回归待 Mac |
| 7. 悄悄话 | 发送、额度、吃瓜、换一个、收到、回应、已读 | 与 Android 完整流程对照 | 已完成主体，真机回归待 Mac |
| 8. 钱包/VIP/签到 | 余额、流水、广告任务、充值现有后端、VIP 状态/记录、签到日历 | 契约与页面状态检查 | 已完成主体 |
| 9. 我的与系统能力 | 资料、头像、皮肤、通知、隐私、协议、缓存、注销、启动页 | iOS 14/最新系统双端检查 | 已完成主体 |
| 10. 管理端 | 用户、举报、广告、广播、配置、统计、日志、流水、支付工具 | 危险操作保护与接口方法核验 | 已完成主体，Mac 回归待办 |
| 11. 视觉统一 | 皮肤透明背景、深色模式、Dynamic Type、VoiceOver、原生 Liquid Glass | 截图与辅助功能检查 | 代码已完成主体，待真机视觉验收 |
| 12. 交付验证 | 静态检查、模型/Endpoint 测试、Mac 编译、iOS 14/最新系统测试、Archive | Xcode Test + Archive 成功 | Windows 静态/资源/plist 已通过；Mac 待执行 |

## 实施顺序

1. 每次先从 Android 当前源码重新生成差异清单，不依据旧截图或旧接口记忆。
2. 先修会导致请求失败、错误扣费、越权或数据破坏的问题。
3. 再修页面结构、状态流、确认弹窗、空态和刷新行为。
4. 最后处理动画、间距、相对时间、辅助功能和系统版本差异。
5. 每轮运行静态检查和契约测试；Mac 上运行真实 Swift 编译后，逐条清零编译诊断。

## 平台差异处理原则

- Android APK 更新接口不能用于 iOS；iOS 更新只走 TestFlight、App Store 或受控 IPA 分发页。
- 后端未提供微信/QQ 路由，Google 又要求真实 `idToken`；iOS 禁止模拟登录，未配置时只显示明确提示。
- 公开 App Store 的数字内容和虚拟货币必须按 Apple 规则接 StoreKit。由于本任务禁止改后端，现有支付能力只原样保留给受控分发测试，并在交付文档中标出上架风险。
- 自动反向地理编码会把精确位置发给第三方地图服务；未获得产品与隐私授权前，只允许用户手动填写位置说明。

## 最终验收清单

- `xcodegen dump` 无警告，项目生成成功。
- Debug/Release 均以 Swift 6 完整并发检查通过。
- iOS 14 模拟器、最新 iOS 模拟器、至少一台真机启动与核心流程通过。
- 所有接口状态：成功、空数据、401、403、429、5xx、离线、超时均有正确界面。
- 关键危险操作均有确认；管理员和系统内置数据有硬保护。
- PrivacyInfo、权限文案、签名能力、Bundle ID、推送环境与分发方式一致。
- Archive 成功，Organizer 校验通过，并按目标渠道导出 IPA 或上传 TestFlight。
